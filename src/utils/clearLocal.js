/**
 * @NenoSann
 * @description This function will clean all local stored info and data,
 *              used in setting.vue for debug or fix something.
 */
function clearLocal() {
  sessionStorage.clear();
  localStorage.clear();
}
